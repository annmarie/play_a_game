// Player constants
export const PLAYERS = {
  ONE: 'Red',
  TWO: 'Yellow',
};

// Legacy exports for tests
export const PLAYER_ONE = PLAYERS.ONE;
export const PLAYER_TWO = PLAYERS.TWO;

// Game messages
export const MESSAGES = {
  DRAW: 'It\'s a draw!',
};

// Legacy export for tests
export const DRAW_MESSAGE = MESSAGES.DRAW;

// Button text constants
export const BUTTON_TEXT = {
  UNDO: 'UNDO',
  RESET: 'RESET',
};

// Game configuration
export const GAME_CONFIG = {
  MAX_HISTORY: 3,
};

// Legacy export for slice
export const MAX_HISTORY = GAME_CONFIG.MAX_HISTORY;

// Legacy exports for button text
export const UNDO_BUTTON_TEXT = BUTTON_TEXT.UNDO;
export const RESET_BUTTON_TEXT = BUTTON_TEXT.RESET;
